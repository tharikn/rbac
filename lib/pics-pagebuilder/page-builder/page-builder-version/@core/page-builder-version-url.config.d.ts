export declare class PageBuilderVersionURL {
    static EndPoints: {
        page_config: {
            pageVersion: string;
            activateVersion: string;
            versionList: string;
            updateVersion: string;
            copyVersion: string;
        };
    };
}
